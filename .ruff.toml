# Ruff Configuration
# This file contains the same ruff configuration as pyproject.toml
# but in a dedicated file for those who prefer it.
# You can use either this file OR the [tool.ruff] section in pyproject.toml

# Uncomment below if you want to use this file instead of pyproject.toml
# Just comment out the [tool.ruff] section in pyproject.toml

# target-version = "py314"
# line-length = 100
# 
# [lint]
# select = ["E", "W", "F", "I", "N", "UP", "B", "C4", "SIM", "Q"]
# ignore = ["E501", "B008", "N802"]
# 
# [format]
# quote-style = "double"
# indent-style = "space"
